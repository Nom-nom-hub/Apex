{
  "name": "@apex/cli",
  "version": "0.0.1",
  "description": "CLI for Apex framework",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "apex": "./bin/apex.js"
  },
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "@apex/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^0.34.0"
  }
}